class Team {
  constructor(team) {
    const name = team.area;
    this.area = { name };
    this.name = team.name;
    this.tla = team.name;
    this.website = team.name;
    this.email = team.name;
    this.id = team.id;
    this.crestUrl = team.crestUrl;
  }
}

export default Team;
