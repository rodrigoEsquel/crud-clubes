class Team {
  constructor(team) {
    this.area.name = team.area;
    this.name = team.name;
    this.tla = team.name;
    this.website = team.name;
    this.email = team.name;
    this.id = team.id;
  }
}

export default Team;
